import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class AppColors {
  // Private constructor to prevent instantiation
  AppColors._();

  static const Color primaryColor = Color.fromARGB(255, 238, 0, 107);
  static const Color lightModeBackground = Colors.white;
  static const Color lightModeTextColor = Colors.black;


  static const Color secondaryColor = Color.fromARGB(255, 177, 11, 232);
}
